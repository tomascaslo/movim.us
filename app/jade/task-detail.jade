div(class="row")
	div(class="col-md-7")
		div
			img(ng-src="{{ task.image }}", alt='..')
			h1 {{ task.name }}
			body {{ task.description }}
			p {{ test }}
			h5 Lugar
			google-map(center="map.center", zoom="map.zoom") 
	div(class="col-md-3 col-md-offset-1")
			h6 4
			h6 {{  }}
			body interesados
			h6 $70
			body acumulado
			h6 10
			body dias restantes
			br
			br
			button(type="button" class="btn-default") Apoyar
